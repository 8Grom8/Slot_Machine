import React from 'react';

import './Header.scss'
import Navbar from "../Navbar";

const Header = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Header;
